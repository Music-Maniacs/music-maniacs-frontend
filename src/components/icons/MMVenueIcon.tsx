import React from 'react';
import { IconBaseProps } from 'react-icons';
import { PiMapPinLine } from 'react-icons/pi';

export const MMVenueIcon = (props: IconBaseProps) => {
  return <PiMapPinLine {...props} />;
};
